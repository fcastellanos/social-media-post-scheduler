# Frontend (Vite + React)

Minimal instructions to get the frontend running locally.

Prerequisites

- Node.js (LTS recommended)
- npm or yarn

Quick start

```bash
# from repository root
cd frontend
npm install
npm run dev
```

Open the app at the Vite dev URL shown in the terminal (commonly `http://localhost:5173`).
